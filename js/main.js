let liczba1='JavaScript';
let liczba2='Jestem świetnym programistą';

function multiply(liczba1, liczba2) {
    let result = liczba1 * liczba2
    if (statesUS.length > 50) {
        statesUS.length=50
     }
    }

multiply (liczba1, liczba2);
